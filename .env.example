PIMLICO_API_KEY = "<PIMLICO_API_KEY>"
# (v2: legacy NEXT_PUBLIC_REGISTRY_ADDRESS removed - replaced by the encrypted directory below)

# v2: Argon2id-encrypted username directory (R1DODirectory.sol).
# OPTIONAL - login never depends on it; it only powers pay-by-username.
# Deploy with: DEPLOYER_PRIVATE_KEY=0x... node scripts/deploy-directory.mjs
NEXT_PUBLIC_DIRECTORY_ADDRESS = "<R1DO_DIRECTORY_ADDRESS>"
