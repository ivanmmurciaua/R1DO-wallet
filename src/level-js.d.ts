// Third-party modules without type declarations, used only inside the lazy
// Railgun pool adapter:
//   · level-js — browser LevelDOWN backend for the Railgun engine
//   · snarkjs  — Groth16 prover injected into the SDK
declare module "level-js";
declare module "snarkjs";
